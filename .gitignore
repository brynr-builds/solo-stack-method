# Node
node_modules/

# Next.js
.next/
out/

# Builds (output dirs). NOTE: anchored to root so they don't match source dirs literally
# named "build" (e.g. web/app/build, web/lib/build — the /build product flow).
/dist/
/build/
**/.next/
coverage/
.turbo/
.cache/
*.log
*.tsbuildinfo

# Env
.env*
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store
